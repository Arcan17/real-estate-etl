# Core pipeline, API and tests — install this for development and CI
pandas>=2.0
polars>=0.20
duckdb>=0.10
httpx>=0.27
pyarrow>=15.0
pytest>=8.0
ruff>=0.4
plotly>=5.0
streamlit>=1.35
numpy>=1.24
fastapi>=0.111
uvicorn>=0.29
openpyxl>=3.1

# Scraper (requires camoufox browser download — not needed for tests or API)
# Install separately before running the ETL pipeline:
#   pip install scrapling>=0.4
